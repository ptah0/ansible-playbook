all:
  hosts:
  children:
    server:
      hosts:
        server.example.com:
  vars:
    ansible_user: example_user # the use you are planning to use, non-root is recomended
    skip_zsh: false # the flag to bypass zsh installation
